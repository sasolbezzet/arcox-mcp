# `@arcox/cli`

Wrapper CLI untuk user/agent terminal.

Entrypoint:

```bash
node packages/cli/bin/arcox.mjs
```

Untuk penggunaan harian dari root monorepo:

```bash
npm run codex-agent -- "send 1 USDC from circle wallet to 0x..."
```
