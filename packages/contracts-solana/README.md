# `@arcox/contracts-solana`

Workspace router Solana Devnet ARCOX.

Isi package ini berasal dari `solana-router`. Runtime deploy helper masih ada di `packages/runtime/scripts/deploy-solana-router.mjs`.

Perintah deploy aktif:

```bash
cd /home/ubuntu/arcox-mcp
npm run deploy:solana-router
```
